{
  "name": "sauti",
  "private": true,
  "version": "0.0.0",
  "description": "Sauti: a domain-agnostic multi-party audio calling library",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "purity": "pnpm -r purity"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "@types/node": "^20.16.11"
  }
}
